FROM python:3.11-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install -r requirements.txt
COPY services /app/services
ENV PYTHONPATH=/app
CMD ["uvicorn", "services.mcp.server:app", "--host", "0.0.0.0", "--port", "7000"]
